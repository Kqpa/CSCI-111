public static boolean findItem<T>(Stack s, T item)
    Create a queue
    create boolean flag to indicate if item has been found
    while (stack is not empty)
    {
        insert the returned value of the pop 
    }
}